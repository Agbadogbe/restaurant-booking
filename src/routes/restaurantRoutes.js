// src/routes/restaurantRoutes.js

const express = require('express');
const router = express.Router();

// Routes Restaurant à définir ici (POST /restaurants, GET /restaurants/:id, etc.)

module.exports = router;