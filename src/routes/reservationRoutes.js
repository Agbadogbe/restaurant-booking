// src/routes/reservationRoutes.js

const express = require('express');
const router = express.Router();

// Routes Réservation à définir ici (POST /reservations, GET /reservations/user/:id, etc.)

module.exports = router;